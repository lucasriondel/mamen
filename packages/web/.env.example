# Base URL for the @mamen/api backend, consumed by @mamen/sdk's HTTP client.
# Leave empty to use the same-origin `/api` prefix (Vite proxies it to the API
# dev server — see vite.config.ts). Set it only when the API is elsewhere.
VITE_API_URL=
