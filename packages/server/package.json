{
	"name": "@mamen/server",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "mkdir -p ../../logs && NODE_ENV=development bun --watch src/index.ts 2>&1 | tee ../../logs/server.log",
		"start": "NODE_ENV=production bun src/index.ts",
		"build": "tsc",
		"test": "bunx --bun vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@fastify/multipart": "^9.4.0",
		"@fastify/static": "^9.0.0",
		"@mamen/shared": "workspace:*",
		"fastify": "^5.7.4",
		"fastify-plugin": "^5.1.0"
	},
	"devDependencies": {
		"bun-types": "^1.3.9",
		"vitest": "^4.0.18"
	}
}
