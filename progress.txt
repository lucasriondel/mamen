## Progress Log

### Task 1: Theme & CSS Changes (PRD Section 1) ✅
- Updated dark theme sidebar variables with cool-blue tint and muted blue-purple accent indicator
- Updated light theme sidebar with faint cool white and blue-purple accent indicator
- Registered `--color-sidebar-indicator` in @theme inline block
- Added `.sidebar-nav-icon-active { filter: none; }` to reduced-motion media query
- TypeScript: clean (0 errors)
- Tests: 141 files, 1432 tests all passing

### Task 2: Sidebar Redesign (PRD Section 2) ✅
- Added active state indicator: 3px rounded pill on left edge using `before:` pseudo-element with `bg-sidebar-indicator`
- Added `is-active` class marker for group-based icon glow targeting
- Implemented icon glow on active: subtle drop-shadow using `group-[.is-active]:drop-shadow-[...]` on icon wrapper spans
- Updated hover effects: `hover:bg-sidebar-accent/60 hover:translate-x-0.5 transition-all duration-200`
- Changed sidebar bg from `bg-card` to `bg-sidebar` and border to `border-sidebar-border`
- Changed text color from `text-muted-foreground` to `text-sidebar-foreground` for nav items
- Added inset separator (`border-t border-sidebar-border mx-3`) between main nav and filter items
- Updated stats section border to use `border-sidebar-border`
- Extracted `baseLinkClass` and `activeIndicatorClass` constants for DRY nav item styling
- Updated Layout tests to match new `bg-sidebar-accent` active class (was `bg-accent`)
- Note: Collapse-related features (label transitions, tooltips, stats collapse) deferred — no collapse mechanism exists yet
- TypeScript: clean (0 errors)
- Tests: 141 files, 1432 tests all passing

### Task 3: Layout Content Panel & Header (PRD Sections 3-4) ✅
- Added `bg-sidebar` to outer `flex h-screen` container so sidebar background extends behind content panel
- Added `rounded-l-2xl bg-background` to content panel for rounded left edge over sidebar bg
- Added layered two-shadow elevation: `shadow-[-2px_0_16px_rgba(0,0,0,0.2),-8px_0_40px_rgba(0,0,0,0.15)]`
- Softened header bottom border from `border-b` to `border-b border-border/30`
- TypeScript: clean (0 errors)
- Tests: 141 files, 1432 tests all passing

### Task 4: Transaction List Entrance Animation (PRD Section 5) ✅
- Added `@keyframes tx-row-enter` CSS animation (opacity 0→1, translateY 6px→0, 350ms ease-out)
- Added `.tx-row-enter` class with `animation-delay: var(--row-delay)` for staggered timing
- Added `.tx-row-enter` to reduced-motion media query (animation: none, opacity: 1)
- Split virtual rows into outer (positioning) + inner (content + animation) divs to avoid transform conflict
- Added entrance animation state: `isEntering` flag with 1.2s auto-disable, `hasAnimatedEntranceRef` for one-shot
- Stagger: 30ms per row × 25 rows max = 750ms total stagger, 350ms per-row animation
- Safe `matchMedia` check for jsdom compatibility in tests
- TypeScript: clean (0 errors)
- Tests: 141 files, 1432 tests all passing

### Task 5: CategoryPicker — Feature 1: Search shows nested categories ✅
- Pulled `categoriesWithSubs` from `useCategories()` in CategoryPicker
- Derived `isSearching = search.length > 0 && !selectedParent` flag
- Added third rendering branch: when searching, renders flat grouped list (mirrors QuickCategoryPicker pattern)
- Each parent rendered as `CommandGroup` with heading; parent as `CommandItem` with `value="{name} (general)"`; subcategories as `CommandItem` with `value="{parent} {sub}"` for cmdk matching
- Clicking parent/subcategory in search mode calls `onSelect` directly (no drill-down)
- Updated "filters categories by search" test: verifies flat search view shows Shopping + subcategories, Dining items not selectable
- Added "finds subcategories by name from parent view" test: typing "Groceries" surfaces it under Shopping group
- Added "calls onSelect directly when clicking subcategory in search view" test
- TypeScript: clean (0 errors)
- Tests: 13 CategoryPicker tests all passing

### Task 6: CategoryPicker — Feature 2: Parent category selector in Add Category modal ✅
- Added `defaultName?: string` prop to `CategoryFormModalProps`
- Added `selectedParentId` state (initialized from `parentId` prop) and `parentPickerOpen` state
- Used `getCategoryById` from `useCategories()` to resolve selected parent for display
- Synced `selectedParentId` with `parentId` prop in the existing `useEffect` (on mode/parentId/defaultName change)
- Pre-fills name field with `defaultName ?? ""` on create mode reset
- Added parent selector field between Name and Color/Icon fields (only in create mode):
  - Popover trigger button shows selected parent name + color dot, or "Root level (no parent)"
  - PopoverContent contains `CategoryPicker` with `allowSubcategory={false}` and `allowCreate={false}`
  - "Remove parent" link to reset to root level when a parent is selected
- Updated `handleSubmit` to use `selectedParentId` instead of the `parentId` prop
- Updated dialog description to use `selectedParentId` instead of prop
- Added `allowCreate?: boolean` prop to `CategoryPickerProps` type (accepted but unused until Feature 3)
- No caller changes needed — `CategoriesPage` still passes `parentId` as before
- TypeScript: clean (0 errors)
- Tests: 141 files, 1434 tests all passing
