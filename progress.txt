Change 1: Remove "New" / "New Merchant" from Transaction Row — DONE
- Removed NewMerchantBadge import from columns.tsx
- Removed NewMerchantBadge render from Description cell in columns.tsx
- Removed createdAt from TransactionTableMeta type in columns.tsx
- Removed createdAt from merchants map builder in index.tsx
- TypeScript type check passes cleanly

Change 2: Show Merchant Name When Matched — DONE
- Updated rawMerchantString cell in columns.tsx
- Matched merchants: MerchantAvatar + merchant name (font-medium) + raw string (text-xs muted) in stacked layout
- Unmatched merchants: only rawMerchantString displayed, no avatar
- TypeScript type check passes cleanly

Change 3: Add Account Column — DONE
- Added getAccountName to TransactionTableMeta type in columns.tsx
- Updated accountId column def with header "Account" and cell renderer using meta.getAccountName in columns.tsx
- Built accountsMap from already-fetched accounts in index.tsx
- Created getAccountName callback in index.tsx
- Added getAccountName to tableMeta in index.tsx
- Removed columnVisibility: { accountId: false } to make column visible in index.tsx
- Added header and cell width classes (w-24) for accountId column in index.tsx
- TypeScript type check passes cleanly

Change 4: Category Badge Click Opens Popover CategoryPicker — DONE
- Added onAssignCategory to TransactionTableMeta type in columns.tsx
- Added imports for useState, CategoryPicker, Popover/PopoverContent/PopoverTrigger in columns.tsx
- Extracted InlineCategoryCell component with useState for popover open/close state in columns.tsx
- Wrapped CategoryBadge, Unmatched badge, and Matched badge in Popover with CategoryPicker in columns.tsx
- Added e.stopPropagation() on popover triggers to prevent row click interference
- Simplified category column cell to delegate to InlineCategoryCell in columns.tsx
- Passed assignCategory (from useQuickCategoryAssign) as onAssignCategory in tableMeta in index.tsx
- TypeScript type check passes cleanly

Change 5: Transaction Detail Side View (Route Outlet) — DONE
- Created routes/transactions.$transactionId.tsx child route rendering TransactionDetailPanel
- Created features/transactions/components/TransactionDetailPanel/index.tsx with:
  - Amount hero display with refund badge
  - Merchant info section (avatar + name for matched, Building2 icon for unmatched)
  - Detail rows for date, account, and category (using CategoryBadge)
  - Anomaly flags display
  - Import metadata section (import month, imported date, duplicate excluded badge)
  - Close button navigating back to /transactions
- Modified routes/transactions.tsx:
  - Added useMatch for /transactions/$transactionId
  - Renders horizontal layout: list (flex-1) + side panel (w-[400px] border-l) when detail match exists
  - Moved -m-6 from TransactionsPage to layout wrapper
- Modified TransactionDataTable/index.tsx:
  - handleRowClick navigates to /transactions/$transactionId when no selection active
  - Selection mode (selectionCount > 0) still toggles row selection as before
- Ran route codegen to update routeTree.gen.ts with new /transactions/$transactionId route
- TypeScript type check passes cleanly
