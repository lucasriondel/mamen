## Fastify Migration Progress

### Step 1+2: Add dependencies & Create core infrastructure ✅
- Added fastify@5.7.4, fastify-plugin@5.1.0, @fastify/static@9.0.0
- Created `src/app.ts` with `buildApp()` factory pattern
  - TypeScript module augmentation for `fastify.db`
  - DB injection via `app.decorate('db', db)` — tests can pass in-memory DB
  - Global error handler
- Created `src/plugins/date-parser.ts` — JSON content-type parser with dateReviver
- Created `src/plugins/static-files.ts` — @fastify/static + SPA fallback
- Verified: typecheck passes, buildApp() works with in-memory DB
- Next: Step 3 — Convert route modules to Fastify plugins
