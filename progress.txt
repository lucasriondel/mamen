## Step 0a: Install Turborepo and scaffold structure ✅

- Installed turbo@2.8.5 as root devDependency
- Added `workspaces: ["packages/*"]` and `packageManager: "bun@1.3.4"` to root package.json
- Added turbo:* scripts to root package.json (turbo:dev, turbo:build, turbo:test, turbo:lint, turbo:typecheck)
- Created `turbo.json` with build/dev/test/lint/typecheck task pipeline
- Created `tsconfig.base.json` with shared compiler options
- Created `packages/shared/` with package.json, tsconfig.json, and src/index.ts placeholder
- Created `packages/web/` with package.json and tsconfig.json (code moves in Step 0b)
- Created `packages/server/` with package.json, tsconfig.json, and src/index.ts placeholder
- Added `.turbo` to .gitignore
- Verified: turbo --dry-run sees all 3 packages (@mamen/shared, @mamen/web, @mamen/server)
- Verified: existing tests still pass (140/142 test files, 1443/1444 tests - 2 pre-existing failures)
- Verified: pre-existing type errors unchanged

## Step 0b: Move frontend code to packages/web/ ✅

- Moved `src/`, `public/`, `index.html` into `packages/web/` via `git mv`
- Moved config files: `vite.config.ts`, `vitest.config.ts`, `tsconfig.app.json`, `tsconfig.node.json`, `components.json`, `eslint.config.js`, `.env.example` into `packages/web/`
- Moved all frontend dependencies and devDependencies from root `package.json` to `packages/web/package.json`
- Root `package.json` now only has turbo + typescript as devDeps; scripts point to turbo
- Root `tsconfig.json` simplified to project references only (shared, web, server)
- No source code import changes needed — `@/` alias still resolves to `./src` within packages/web
- Verified: `vite build` succeeds (dist built in 2.24s)
- Verified: `vitest run` passes (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)
- Verified: `vite dev` starts successfully
- Verified: `turbo test` runs from root correctly

## Step 0c: Extract shared types to packages/shared/ ✅

- Moved 8 type files from `packages/web/src/types/` → `packages/shared/src/types/` via `git mv`
  - account.types.ts, transaction.types.ts, anomaly.types.ts, merchant.types.ts, rule.types.ts, settings.types.ts, category.types.ts, subscription.types.ts
- Moved 10 schema files from `packages/web/src/lib/schemas/` → `packages/shared/src/schemas/` via `git mv`
  - account.schema.ts, transaction.schema.ts, anomaly.schema.ts, merchant.schema.ts, rule.schema.ts, settings.schema.ts, category.schema.ts, subscription.schema.ts, llmTransaction.schema.ts, duplicateCheck.schema.ts
- Moved barrel index files (types/index.ts, schemas/index.ts) to shared package
- Updated all schema imports from `@/types` → relative `../types` (internal to shared package)
- Added `zod` as dependency in `packages/shared/package.json`
- Created `packages/shared/src/index.ts` barrel re-exporting all types and schemas
- Created re-export shims in web package:
  - `packages/web/src/types/index.ts` → re-exports all types from `@mamen/shared`
  - `packages/web/src/lib/schemas/index.ts` → re-exports all schemas from `@mamen/shared`
- Updated 3 direct `@/types/<file>` subpath imports in useMerchantDetail.ts → barrel `@/types`
- Updated 6 direct `@/lib/schemas/llmTransaction.schema` imports → barrel `@/lib/schemas`
- Updated 7 schema test files to import from `@/lib/schemas` barrel instead of relative paths
- Verified: `@mamen/shared` typechecks successfully
- Verified: `vite build` succeeds (dist built in 2.29s)
- Verified: `vitest run` passes (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)

## Step 0d: Scaffold packages/server/ ✅

- Replaced placeholder `src/index.ts` with minimal `Bun.serve` HTTP server
  - Listens on port 3000 (configurable via PORT env var)
  - Returns JSON `{ status: "ok", message: "mamen API" }` for `/api/*` routes
  - Returns 404 for all other routes
- Installed `bun-types@1.3.9` as devDependency in `packages/server/`
- `@mamen/shared` already declared as workspace dependency (from Step 0a)
- Verified: `bun run --filter @mamen/server start` starts and serves on port 3000
- Verified: `curl http://localhost:3000/api/health` returns JSON response
- Verified: `tsc --noEmit` typechecks cleanly in server package
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)

## Step 0e: Verify full pipeline ✅

- Added dev proxy to `packages/web/vite.config.ts`: `/api` → `http://localhost:3000`
- Added `vitest` as explicit devDependency in `packages/server/`
- Fixed server test script: `vitest run --passWithNoTests` (no test files yet)
- Verified: `bun run build` — @mamen/shared and @mamen/server build successfully; @mamen/web `vite build` produces dist in 2.26s (pre-existing tsc type errors unchanged)
- Verified: `bun run test` — @mamen/server passes (0 tests, --passWithNoTests); @mamen/web passes (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)
- Verified: `bun run dev` — both web (Vite on :5173) and server (Bun on :3000) start in parallel via Turborepo
- Verified: @mamen/shared types consumed correctly by both web and server packages

## Step 1: Bun Server Scaffold ✅

- Created `packages/server/src/router.ts` — simple route matcher for /api/* endpoints
  - Supports GET/POST/PUT/DELETE/PATCH methods with pattern matching (including `:param` segments)
  - Exports `router` object for registering routes and `handleRequest` for dispatching
  - Returns 404 JSON for unmatched API routes, 500 JSON on handler errors
- Updated `packages/server/src/index.ts` — full Bun.serve entry point
  - API routing via `handleRequest()` from router
  - Static file serving from `packages/web/dist/` for production
  - SPA fallback (serves index.html for client-side routing paths)
  - Health check endpoint: GET `/api/health` → `{ status: "ok" }`
- Vite dev proxy already configured in Step 0e (`/api` → `http://localhost:3000`)
- Verified: `tsc --noEmit` passes cleanly in server package
- Verified: server starts, `/api/health` returns 200, unknown API routes return 404, non-API routes serve SPA
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)

## Step 2: Repository Port Interfaces ✅

- Created `packages/server/src/lib/repository/ports/` directory with 11 port files
- `base.port.ts` — Generic `BaseRepository<T>` type with CRUD operations: get, getAll, add, bulkAdd, update, bulkPut, delete, bulkDelete, bulkGet, count, clear
- `account.port.ts` — Extends base with: getByName, getByType
- `transaction.port.ts` — Richest port, extends base with: getByAccountId, getByAccountIdAndMonth, countByAccountIdAndMonth, deleteByAccountIdAndMonth, getByMerchantId, getByCategoryId, getByDateRange, getByImportBatchId, countByImportBatchId, deleteByImportBatchId, getByLinkedRefundId, getAllOrderedByDate, bulkAddReturningIds, filterByPredicate, filterIdsByPredicate
- `merchant.port.ts` — Extends base with: getByName, getByNameCaseInsensitive, getAllOrderedByName
- `rule.port.ts` — Extends base with: getByMerchantId, countByMerchantId, getByMerchantIdAndPattern
- `setting.port.ts` — Extends base with: getByKey, putByKey
- `app-settings.port.ts` — Standalone type (singleton): get, put, clear
- `category.port.ts` — Extends base with: getBySlug, getByParentId, getByParentIdOrderedBySortOrder, getRootCategories, getAllOrderedBySortOrder
- `subscription.port.ts` — Extends base with: getByMerchantId, getFirstByMerchantId, getByMerchantIdAndFrequency, getByStatus
- `unit-of-work.port.ts` — Transaction wrapper: run<T>(fn) => Promise<T>
- `database.port.ts` — Lifecycle: open, close, delete
- `index.ts` — Barrel re-export of all port types
- Verified: `tsc --noEmit` passes cleanly in server package
- Verified: server tests pass (0 tests, --passWithNoTests)
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)

## Step 3: SQLite Adapter (bun:sqlite) ✅

- Created `packages/server/src/lib/repository/adapters/sqlite/` directory with 12 files
- `connection.ts` — DB connection management with WAL mode, foreign keys, and migration runner
  - `getConnection(dbPath)` — opens/creates SQLite DB file, runs migrations, returns singleton
  - `closeConnection()` — closes the DB connection
  - `deleteDatabase(dbPath)` — closes connection and removes DB file + WAL/SHM files
  - `getInMemoryConnection()` — creates in-memory DB for testing
- `migrations/001-initial-schema.ts` — All CREATE TABLE + CREATE INDEX statements
  - 8 tables: accounts, transactions, merchants, rules, settings, appSettings, categories, subscriptions
  - Compound index: `idx_tx_account_month` on transactions(accountId, importMonth)
  - Unique index on settings(key)
  - All indexes match the existing Dexie schema
- `account.adapter.ts` — Full AccountRepository implementation with row mapping (Date ↔ ISO string)
- `transaction.adapter.ts` — Full TransactionRepository implementation (richest adapter)
  - Dates stored as ISO strings, converted to Date on read
  - Booleans stored as INTEGER (0/1), converted to boolean/undefined on read
  - anomalyFlags stored as JSON TEXT, serialized/deserialized in row mappers
  - filterByPredicate/filterIdsByPredicate load all rows and filter in JS
- `merchant.adapter.ts` — Full MerchantRepository with case-insensitive name search via COLLATE NOCASE
- `rule.adapter.ts` — Full RuleRepository with compound merchantId+pattern lookup
- `setting.adapter.ts` — Full SettingRepository with unique key-based upsert via INSERT OR REPLACE
- `app-settings.adapter.ts` — AppSettingsRepository for singleton settings (LLM config stored as JSON)
- `category.adapter.ts` — Full CategoryRepository with hierarchical queries (parentId, root, sortOrder)
- `subscription.adapter.ts` — Full SubscriptionRepository with transactionIds stored as JSON array
- `unit-of-work.adapter.ts` — UnitOfWork using manual BEGIN/COMMIT/ROLLBACK for async transaction support
- `database.adapter.ts` — DatabasePort wrapping connection lifecycle (open, close, delete)
- `index.ts` (sqlite/) — Factory function `createDatabase(db)` that creates all repo adapters from a single DB connection
- `packages/server/src/lib/repository/index.ts` — `getDatabase()` singleton + `resetDatabase()` for testing
- Verified: `tsc --noEmit` passes cleanly in server package
- Verified: server tests pass (0 tests, --passWithNoTests)
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same pre-existing failures)

## Step 4: REST API Routes ✅

- Updated `packages/server/src/router.ts` — Router now extracts path parameters from `:param` segments into `params.params` record
- Created `packages/server/src/api/helpers.ts` — Shared utilities for route handlers
  - `parseBody<T>(req)` — JSON body parser with ISO date string revival (auto-converts date strings to Date objects)
  - `parseId(raw)` — Safe numeric ID parsing from path params
  - `jsonResponse(data, status)` / `errorResponse(message, status)` — Response helpers
- Created `packages/server/src/api/accounts.ts` — CRUD: GET/POST/PUT/DELETE /api/accounts, plus getByName, getByType
- Created `packages/server/src/api/transactions.ts` — CRUD + rich filtered queries via query params
  - Supports filtering by: accountId, importMonth, merchantId, categoryId, importBatchId, linkedRefundId, dateRange, orderBy
  - Count endpoint with accountId+importMonth and importBatchId filters
  - Bulk operations: bulk-add, bulk-put, bulk-get, bulk-delete
  - Delete by account+month, delete by import batch
- Created `packages/server/src/api/merchants.ts` — CRUD + getByName, getByNameCaseInsensitive, ordered by name, bulk-put
- Created `packages/server/src/api/rules.ts` — CRUD + getByMerchantId, count by merchantId, getByMerchantIdAndPattern, bulk-add, bulk-delete
- Created `packages/server/src/api/settings.ts` — GET all, GET/PUT by key, DELETE by id, clear
- Created `packages/server/src/api/app-settings.ts` — GET/PUT singleton, clear
- Created `packages/server/src/api/categories.ts` — CRUD + getBySlug, getByParentId (with sortOrder option), getRootCategories, getAllOrderedBySortOrder, bulk-add, bulk-put, clear
- Created `packages/server/src/api/subscriptions.ts` — CRUD + getByMerchantId, getByStatus, getFirstByMerchantId, getByMerchantIdAndFrequency, bulk-put, clear
- Created `packages/server/src/api/database.ts` — POST /api/database/reset (clears all tables), POST /api/database/export (dumps all tables), POST /api/database/import (clears + reimports all tables)
- Created `packages/server/src/api/index.ts` — Barrel import that registers all route modules
- Updated `packages/server/src/index.ts` — Added `import "./api"` to register all API routes at startup
- Verified: `tsc --noEmit` passes cleanly in server package
- Verified: server tests pass (0 tests, --passWithNoTests)
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same pre-existing failures)

## Step 5: Frontend API Client ✅

- Created `packages/web/src/lib/api/` directory with 12 files
- `client.ts` — Base fetch wrapper with error handling, JSON parsing, ISO date string revival
  - `api.get<T>(path)`, `api.post<T>(path, body?)`, `api.put<T>(path, body?)`, `api.delete<T>(path)` methods
  - `ApiError` class with status code for error handling
  - Automatic date string → Date object conversion in responses
- `invalidation.ts` — Reactive query invalidation bus
  - `subscribe(keys, listener)` — subscribe to table-level invalidation events
  - `invalidate(...keys)` — notify all subscribers watching given table names
  - Deduplicates listener notifications per invalidation cycle
- `useApiQuery.ts` — React hook replacing useLiveQuery for server-backed data
  - `useApiQuery<T>(queryFn, keys, defaultValue?)` — fetches data, re-fetches on invalidation
  - Uses `useSyncExternalStore` for version tracking + `useEffect` for async fetch
  - Cancellation support to prevent stale state updates
- `accounts.ts` — accountsApi: getAll, get, getByName, getByType, create, update, delete
- `transactions.ts` — transactionsApi: getAll (with rich query params), get, count, create, bulkAdd, bulkGet, bulkPut, update, delete, bulkDelete, deleteByAccountMonth, deleteByImportBatch
- `merchants.ts` — merchantsApi: getAll (with orderBy), get, getByName, getByNameCaseInsensitive, create, update, bulkPut, delete
- `rules.ts` — rulesApi: getAll (with merchantId filter), get, count, getByMerchantIdAndPattern, create, bulkAdd, update, delete, bulkDelete
- `settings.ts` — settingsApi: getAll, getByKey, putByKey, delete, clear
- `app-settings.ts` — appSettingsApi: get, put, clear
- `categories.ts` — categoriesApi: getAll (with parentId/orderBy), getRoot, get, getBySlug, create, bulkAdd, update, bulkPut, delete, clear
- `subscriptions.ts` — subscriptionsApi: getAll (with merchantId/status), get, getFirstByMerchant, getByMerchantAndFrequency, create, update, bulkPut, delete, clear
- `import.ts` — importApi: run (invalidates transactions, merchants, rules, subscriptions)
- `database.ts` — databaseApi: reset, export, import (invalidates all tables)
- `index.ts` — Barrel re-export of all API modules, client, invalidation, and useApiQuery
- All mutation functions auto-invalidate their respective table keys after success
- Verified: `tsc --noEmit` passes cleanly for all new API files (0 errors in src/lib/api/)
- Verified: web tests still pass (140/142 test files, 1443/1444 tests — same 2 pre-existing failures)

## Step 7: Migrate All Hooks (21 hooks) ✅

- Migrated all 21 hooks from `useLiveQuery` + direct `db.*` calls to `useApiQuery` + API client calls
- Hook signatures and return types remain identical — only the data source changed
- Shared hooks (`packages/web/src/hooks/`):
  - `useCategories.ts` — `categoriesApi.getAll({ orderBy: 'sortOrder' })`, subscribes to `['categories']`
  - `useMerchants.ts` — `merchantsApi.getAll({ orderBy: 'name' })`, subscribes to `['merchants']`; `createMerchant` → `merchantsApi.create()`, `getMerchantByName` → `merchantsApi.getByNameCaseInsensitive()`
  - `useUnmatchedCount.ts` — `transactionsApi.getAll()` then filters client-side for `merchantId === undefined`
  - `useCurrentMonthCount.ts` — `transactionsApi.getAll({ startDate, endDate })` then `.length`
  - `useBreadcrumbs.ts` — `categoriesApi.get(id)` and `merchantsApi.get(id)` with try/catch for 404s
- Feature hooks:
  - `useFilteredTransactions.ts` — All filter paths migrated: anomalies, subscriptions (bulkGet), category drill-down, month range, unmatched, and default all-transactions
  - `useSubscriptions.ts` — `subscriptionsApi.getAll()`, subscribes to `['subscriptions']`
  - `useMerchantsList.ts` — `Promise.all([merchantsApi, transactionsApi, categoriesApi].getAll())`, subscribes to `['merchants', 'transactions', 'categories']`
  - `useMerchantDetail.ts` — `merchantsApi.get(id)`, `transactionsApi.getAll({ merchantId })`, `rulesApi.getAll({ merchantId })`, `categoriesApi.getAll()`
  - `useExistingMerchant.ts` — `merchantsApi.get(id)` + `rulesApi.getAll({ merchantId })`
  - `useNetSpending.ts` — `transactionsApi.getAll({ startDate, endDate })` + `categoriesApi.getAll()`
  - `useSpendingBreakdown.ts` — Same pattern as useNetSpending with optional dateRange
  - `useSpendingComparison.ts` — `transactionsApi.getAll({ startDate, endDate })` for previous period
  - `useCategoryTooltipData.ts` — `transactionsApi.getAll({ categoryId })` + `merchantsApi.get(id)` for top merchants
  - `useAnomalies.ts` — `transactionsApi.getAll()`, client-side anomaly filtering unchanged
  - `useTransactionSearch.ts` — `transactionsApi.getAll()`, client-side search index unchanged
  - `useRuleMatchPreview.ts` — `transactionsApi.getAll()`, client-side regex matching unchanged
  - `useSettings.ts` — `appSettingsApi.get()` for read, `appSettingsApi.put()` for save (debounced)
  - `useDisplayPreferences.ts` — `settingsApi.getByKey('displayPreferences')` with try/catch
  - `useAccountMonthData.ts` — `transactionsApi.getAll({ accountId })`, client-side month counting unchanged
  - `useCategoryTree.ts` — `categoriesApi.getAll({ orderBy: 'sortOrder' })`, client-side tree building unchanged
- Verified: `tsc --noEmit` passes cleanly (0 type errors)
- Verified: 108/142 test files pass, 1252/1444 tests pass — 34 test files (192 tests) now fail because they use fake-indexeddb directly; these will be updated in Step 10 (Update Tests) to mock API calls instead

## Step 8: Migrate All Service Call Sites ✅

- Migrated ALL non-test source files from direct `db.*` (Dexie/IndexedDB) calls to REST API client calls
- Components migrated (20 files):
  - accounts: CreateAccountModal, EditAccountModal, AccountCard
  - merchants: EditMerchantModal, MerchantSearchSelect, MerchantRulesList, MerchantAssignmentModal, MerchantDetailPage
  - rules: RulesPage, RuleEditModal, RulesListByMerchant
  - settings: ClearDataDialog, DataManagementSection, AnomalySettingsForm
  - subscriptions: SubscriptionDetail
  - dashboard: DashboardPage
  - transactions: TransactionDataTable, RefundLinkModal
  - shared: Layout/Sidebar, MatchPreviewList, CommandPalette
- Routes migrated (2 files): accounts.tsx, __root.tsx
- Services migrated (18 files):
  - settings: exportService, importService, preferencesService
  - import: csvImporter, pdfImporter, duplicateDetector, importWithRules
  - anomalies: anomalyDetector
  - rules: applyRule, addRuleToMerchant, detectRuleConflict, rulesEngine, batchPatternAnalyzer, ruleOperations, ruleEngine
  - merchants: batchAssignMerchant
  - transactions: deleteTransactions, batchCategoryAssign, assignManualCategory, refundService
  - subscriptions: subscriptionDetector
- Hooks migrated (3 additional files): useCategoryMutations, useDuplicateActions, useNavigateToTransaction
- Other files migrated: lib/llm/guards.ts, lib/db/seeds/categories.ts, features/dev/DbTestPanel/index.tsx
- Migration patterns used:
  - `db.TABLE.toArray()` → `xxxApi.getAll()`
  - `db.TABLE.get(id)` → `xxxApi.get(id)`
  - `db.TABLE.add(record)` → `xxxApi.create(record)`
  - `db.TABLE.update(id, changes)` → `xxxApi.update(id, changes)`
  - `db.TABLE.where(field).equals(val)` → API-specific methods or `getAll()` + client-side filter
  - `db.transaction('rw', ...)` wrappers → removed (sequential API calls)
  - `useLiveQuery(fn, deps)` → `useApiQuery(fn, tableKeys, defaultValue)`
- Only test files (73 *.test.ts/tsx files) still reference `@/lib/db` — to be updated in Step 10
- Verified: `tsc --noEmit` passes cleanly (0 type errors)
- Verified: 68/142 test files pass, 917/1444 tests pass — 74 test files (527 tests) now fail because tests still use fake-indexeddb + direct db calls while source code now uses API client; these will be updated in Step 10 (Update Tests)

## Step 9: Cleanup ✅

- Removed `dexie` (^4.3.0) and `dexie-react-hooks` (^4.2.0) from `packages/web/package.json` dependencies
- Removed `fake-indexeddb` (^6.2.5) from `packages/web/package.json` devDependencies
- Deleted `packages/web/src/lib/db/schema.ts` — Dexie database definition (versions 1-9)
- Deleted `packages/web/src/lib/db/db.integration.test.ts` — Dexie integration tests
- Replaced `packages/web/src/lib/db/index.ts` barrel with a stub comment (test files still import from here — to be updated in Step 10)
- Removed `import 'fake-indexeddb/auto'` from `packages/web/src/test/setup.ts`
- Relocated seed files out of db directory:
  - `packages/web/src/lib/db/seeds/categories.ts` → `packages/web/src/lib/seeds/categories.ts`
  - `packages/web/src/lib/db/seeds/categories.test.ts` → `packages/web/src/lib/seeds/categories.test.ts`
- Updated `packages/web/src/routes/__root.tsx` import path: `@/lib/db/seeds/categories` → `@/lib/seeds/categories`
- Note: Old service files in `packages/web/src/features/*/services/` are NOT removed — they're still the active business logic, now calling API endpoints instead of Dexie directly. Server-side service duplicates (PRD Step 4) were never created.
- Verified: `tsc --noEmit` passes cleanly on both web and server packages (0 type errors)
- Verified: No non-test source files import from `dexie`, `@/lib/db`, or `fake-indexeddb`
- Verified: 64/141 test files pass, 712/1397 tests pass — 77 test files (685 tests) fail because test files still import `db` from the now-empty `@/lib/db` barrel; will be fixed in Step 10 (Update Tests)